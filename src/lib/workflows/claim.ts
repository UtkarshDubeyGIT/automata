/**
 * @deprecated Prefer importing directly from `./runtime` (the consolidated WorkflowRuntime module).
 * Kept as a compatibility layer for existing callers and external interfaces.
 */
export * from "./runtime";
