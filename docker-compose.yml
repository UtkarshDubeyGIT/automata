services:
  web:
    build: .
    env_file: .env.local
    ports:
      - "3000:3000"
    restart: unless-stopped
  worker:
    build: .
    env_file: .env.local
    command: npm run worker
    restart: unless-stopped
